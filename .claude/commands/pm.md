---
description: Engage the Product Manager agent to clarify a vague request and write a tight one-page spec.
argument-hint: [request to scope, e.g. "add scheduled scans to Kavach"]
---

Use the `Agent` tool with `subagent_type: "product-manager"` to scope the following request. The PM should output a one-page spec (Goal / Why now / In scope / Out of scope / Edge cases / Acceptance criteria / Open questions) and stop. Do not implement anything.

Request: $ARGUMENTS
