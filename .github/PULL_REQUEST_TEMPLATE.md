## What this PR does

<!-- One sentence describing the user-visible change. -->

## Why

<!-- Link to the issue / Jira / context. -->

## Screenshots / GIFs (if UI)

## Checklist

- [ ] `cd backend && go build ./... && go vet ./...` passes
- [ ] `cd frontend && npm run lint && npm run build` passes
- [ ] `docker compose up --build` boots and login works
- [ ] I started a test, watched it live, and downloaded the PDF
- [ ] If schema changed, I added an idempotent migration
- [ ] If API changed, I updated the table in `README.md`
